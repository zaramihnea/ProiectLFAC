type Point START
    x: float;
    y: float;
    distanceToOrigin[]: float START
        return sqrt[x*x + y*y];
    STOP
STOP

var globalVar: int = 10;
var pointObj: Point;

function add[a: int, b: int]: int START
    return a + b;
STOP

function main[] START
    pointObj.x := 3.0;
    pointObj.y := 4.0;

    while (globalVar != 5) {
        print("Global var: ", globalVar);
        globalVar := globalVar - 1;
    }

    var distance = pointObj.distanceToOrigin();
    
    print("Distance to origin: ", distance);
STOP
